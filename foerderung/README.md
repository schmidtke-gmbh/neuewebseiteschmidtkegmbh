# Fördercheck-Funnel · schmidtke-gmbh.de/foerderung/ (Unterseite der Hauptseite)

**Stand v11 (Feedback des Marketing-Coachs, 24.09.2026):** (1) Die Münzen-Illustrationen (wirkten wie Casino) sind weg; stattdessen überall `illu-zuschuss.webp` (weiße Euro-Kachel mit grünem Haken, Higgsfield). (2) **Mitarbeitergewinnung** ist als Thema drin: Hero-Satz „planbar Anfragen, Umsatz und Mitarbeiter“, sechs Themenkarten (neu: „Anzeigen & Reichweite“ mit `illu-anzeigen`, „Mitarbeitergewinnung“ mit `illu-team`), Baustein 6 in „Was genau gefördert wird“, Kachel „Mitarbeiter gewinnen“ in Frage 6, Emrich-Ergebnis („besetzte MTA-Stellen für eine Gesundheitspraxis“) steht in „Über uns · Ergebnisse, die bleiben“. Formulierung „Wir arbeiten aktiv mit Ihnen an Ihrem Marketing“ (Leute wollen sehen, dass gearbeitet wird), aber weiterhin ohne Umsetzungsversprechen. (3) **Hero ist ein echter Start:** Die Karte rechts zeigt Frage 1 („Wo hat Ihr Unternehmen seinen Sitz?“) mit 16 Bundesland-Kacheln; jede ist ein Link `anfragen.html?land=<Bundesland>`. `main.js` liest `?land=` auf der Formularseite, markiert die Kachel, speichert die Antwort und springt zu Frage 2 (bzw. 1b bei Sachsen/Niedersachsen/RLP); „Zurück“ führt zu Frage 1. UTM-Parameter werden mit `&` angehängt, wenn der Link schon ein `?` hat. Unbekannter Wert → Frage 1 wie gewohnt. (4) **Bundesland-Umrisse:** `assets/img/bundeslaender.svg` ist ein Symbol-Sprite (16 `<symbol id="bl-<slug>" viewBox="0 0 64 64">`, aus `deutschland-karte.svg` extrahiert und vereinfacht, 19 KB), Die 16 Symbole sind **inline** direkt nach `<body>` in `index.html` und `anfragen.html` eingebettet (`<svg class="bl-sprite">`) und per `<svg class="bl"><use href="#bl-bayern"/></svg>` in den Hero-Kacheln und in Frage 1 des Förderchecks referenziert; Farbe über CSS `fill`. Inline statt externer Datei, weil Browser externe `<use href="datei.svg#id">`-Referenzen über `file://` (Datei per Doppelklick geöffnet) blockieren und die Umrisse dann leer bleiben. `assets/img/bundeslaender.svg` bleibt als Quelle liegen. (5) **Neue Reihenfolge** (Coach: Ergebnisse und Bewertungen viel weiter oben, Ablauf nur einmal): Hero · Laufband · Ergebnisse · Bewertungen · Für wen · Staatlich gefördert (jetzt mit vier Schritten Erstgespräch / Antrag vor Angebotsabschluss / Informationsschreiben, dann Start / Zuschuss nach Prüfung, die frühere Ablauf-Sektion ist gestrichen) · Was genau gefördert wird · Warum wir · Über uns · Bild-CTA · Kunden · Ihr Berater · FAQ · Abschluss. Nav: Ergebnisse, Bewertungen, Für wen, Förderung (#foerderung), Über uns, FAQ. (6) **Ergebnis bleibt verdeckt (Neugier):** Nach dem Rechenschritt steht nur „Ihr Unternehmen kommt für die Förderung in Frage“, links das Formular („Ihr persönliches Ergebnis“, Button „Ergebnis anfordern“), rechts ein Teaser mit unscharfen Platzhaltern (`•• %`, `•.••• €`, keine echten Zahlen im DOM). Nach dem Absenden verschwinden Formular und Teaser, es kommt „Danke, <Vorname>. Wählen Sie jetzt Ihren Telefontermin, dann bekommen Sie Ihr Ergebnis im Gespräch“ mit Termin (Standard, Cal.com) oder Rückruf; bei Handel 2030 ein grüner Hinweis „Zusätzlich für Sie gefunden … Landesprogramm“ ohne Details. Fördersatz und Zuschuss gehen nur als versteckte Felder `quote`/`zuschuss_je_beratung` mit dem Lead an uns. (7) **Geförderte Beratungen** (Richtlinie): Die Karten mit dem Badge „Beratung mit staatlichem Zuschuss genutzt“ sind weiß, nennen nur Branche und Betrieb (Kampfsportschule Chemnitz, Kampfsport- und Fitnessstudio Vogt, Bettenfachgeschäft Bettenland Alesi – gefördert wurde das Bettenland, nicht der Schlafwissenschaftler) mit Badge, ohne Beratungsthemen und ohne Umsetzungsergebnisse (keine Leads, kein Rebranding, keine Webseite) – sonst sähe es so aus, als wäre Umsetzung über die Förderung abgerechnet worden; vierte Karte „Und viele weitere“ mit Link zum Fördercheck. Umsetzungsergebnisse stehen nur in den drei großen Fallstudien und in der Galerie (ohne Förder-Badge). (8) Die fünf Vertrauensmerkmale sitzen in einem **weißen Siegel-Panel** (`.seal`, Gold-Rot-Linie oben, Kicker „Qualitätsmerkmale“, fünf Spalten mit Trennlinien), damit sie sich von den vier dunklen Schritt-Karten abheben. (9) Kein Video im Hero-Mock, das Beratungs-Video in „Was genau gefördert wird“ bleibt. Kein „Ergebnis sofort“ mehr auf der Seite („Sechs Fragen · keine Anmeldung · zwei Minuten“). Offen: Thomas Glüttet als weitere Referenz.

**Richtlinien-Check (Stand v10, nach KI-Prüfung gegen die Förderrichtlinie):** (1) Keine Aussage „Sie zahlen nur 20–50 %“: Der Kunde zahlt die Rechnung vollständig inkl. MwSt. und erhält den Zuschuss erst nach Prüfung, kein Rechtsanspruch, Ablehnung möglich – so steht es in Hero-Sektion „Staatlich gefördert“, Schritt 3 „Zuschuss nach Prüfung“, Fineprint, FAQ „Muss ich in Vorleistung gehen?“ und im Fördercheck-Ergebnis („kommt für die Förderung in Frage“ statt „ist förderfähig“). Kein „No-Brainer“. (2) Selbstbezeichnung überall „Unternehmensberatung für Marketing und Digitalisierung“, nie „Agentur“ (Richtlinie: überwiegender Geschäftszweck Unternehmensberatung); Footer, Über uns, Vertrauensmerkmal „Seit 2016“ entsprechend. (3) Beratung darf nicht auf den Kauf eigener Leistungen gerichtet sein: Sektion „Warum die Schmidtke GmbH“ beschreibt Praxiserfahrung, keinen Leistungskatalog („Das kennen wir aus der Praxis“, „Woher unsere Praxiserfahrung kommt“); Hinweis „Gefördert wird ausschließlich die Beratung … unabhängig von einer späteren Umsetzung, keine Verpflichtung gegenüber uns“; kein „mit oder ohne uns“. (4) Keine Kunden aus ausgeschlossenen Branchen auf der Seite: Bentivegna (Steuerberatung), Just Brides (Agentur), Meliadis (Webdesign & Marketing) sowie die Bewertungen von Pana Meliadis und Gabriele Buck (Coach) entfernt – 17 Logos, 6 Galerie-Kacheln, 4 Sterne-Bewertungen. (5) Förder-Badges ohne Prozentsatz („Beratung mit staatlichem Zuschuss genutzt“), weil Vogt in Baden-Württemberg 50 % hat.

**Regeln für den Text (Stand v8):** Der Fördercheck ist eine eigene Seite `anfragen.html` (Netlify Pretty URL `/foerderung/anfragen`); alle CTA-Buttons der Landingpage sind Links dorthin, UTM-Parameter und fbclid werden per JS an den Link gehängt. Kein „Vertrag“ mehr, sondern „Angebotsabschluss“ (Antrag vor Angebotsabschluss, Angebot annehmen/abschließen). „Beratung“ sparsamer: Zusammenarbeit, Strategie, geförderter Weg, Projekt, Empfehlung. Die „3.500 € förderfähige Beratungskosten“ stehen nirgends mehr auf der Seite (nur intern in `main.js` als `MAX_KOSTEN_NETTO` für die Rechnung); nach außen geht es nur um 50–80 % Zuschuss und bis zu 2.800 € netto je Beratung, kein Eigenanteil. Sie-Form durchgehend (Ausnahme: wörtliche Kundenzitate). Kein „Beratertag“ mehr, sondern „Beratung“ (der Auftraggeber will nicht den Eindruck „ein Tag und fertig“); Kernsatz: „Wir arbeiten mit Ihnen daran, wie Ihr Unternehmen sichtbar wird und planbar Anfragen gewinnt.“ Der Satz „Der Antrag muss vor Vertragsabschluss stehen“ steht nicht mehr im Hero (nur noch in Ablauf und FAQ). Nichts versprechen, was nur im Umsetzungsangebot steckt (kein „wir übernehmen Dreh und Schnitt“, kein „60 Minuten pro Woche“); die Agenturleistungen werden als Kompetenznachweis gezeigt, nicht als Bestandteil der Beratung. Das Wort „BAFA“ darf nirgends auf der Seite stehen (auch nicht im Title, in der Meta-Description oder im FAQ-Schema); stattdessen „Förderstelle“, „staatliche Beratungsförderung“, „der Bund“. Keine Berater-ID. Auf der Startseite steht nicht, welche Regionen 80 % und welche 50 % bekommen, nur „bis zu 80 %, je nach Standort“; den Satz zeigt erst der Fördercheck. Wiederkehrender Aufhänger: „Sind Sie förderfähig?“

Handcodierte Seiten mit Fragebogen, Rechenschritt, verdecktem Ergebnis, Netlify-Formular und Cal.com-Kalender. Kein Framework, kein Build.

**Ausschluss-Screen (v11e):** Wer nicht förderfähig ist (250+ Mitarbeiter, über 50 Mio. € Umsatz, ausgeschlossene Branche), sieht unter der Begründung eine dunkle Navy-Karte `.result-alt` mit grünem Badge „Kein Problem“, Titel „Wir arbeiten trotzdem mit Ihnen zusammen.“, Hinweis auf das reguläre Formular (Marketing, Videoproduktion, Webseite, Anzeigen, Social Media als Chips) und rotem Primär-Button „Jetzt Anfrage stellen“ → `https://schmidtke-gmbh.de/formular`. Vorher war das eine weiße Karte auf hellem Grund mit Ghost-Button und ging unter.

**Tracking-Kette (v12):** Jede Stufe hat eine eigene Adresse, weil VibeTrack (und Pixel/Analytics) Seitenaufrufe zählen: (1) `/foerderung/anfragen` = Fördercheck gestartet, (2) **`/foerderung/lead`** = Lead (Daten abgegeben; `main.js` setzt die Adresse per `history.pushState`, der VibeTrack-Tracker wertet den Pfadwechsel wie einen Seitenaufruf; Netlify-Rewrite in `netlify.toml` auf `anfragen.html`, damit ein Reload nicht 404 läuft), (3) **`/foerderung/danke`** = voller Lead (Termin in Cal.com gebucht → `danke.html`, ohne Kalender, Sie-Form). Dazu `dataLayer`-Events `funnel_step`, `check_completed`, `lead_submitted`, `call_booked`, `funnel_complete`. Netlify Forms: `foerdercheck` (alle Leads mit allen Antworten als Felder), **`foerdercheck-termin`** (volle Leads: dazu `termin`, `termin_start/-ende`, `antworten`), `foerdercheck-rueckruf`. Die Cal.com wird komplett vorbelegt (geprüft am 24.09. am Event „20 Minuten Gespräch am Telefon“): Name, E-Mail, Telefon (`attendeePhoneNumber` und Ort „Telefonnummer des Teilnehmers“ per `location`-JSON, Nummer als +49…), Thema `title` „Fördercheck: Ergebnis besprechen“ und die Antworten als Notiz (`notes`, ohne Fördersatz). Der Kunde muss im Kalender nur noch bestätigen. Im VibeTrack-Dashboard müssen zwei Ziele angelegt werden: Lead = Seitenaufruf `/foerderung/lead`, Voller Lead = Seitenaufruf `/foerderung/danke`. Die Haupt-Danke-Seite `/danke` (mit Kalender) wird vom Fördercheck nicht mehr benutzt, sonst würde doppelt gebucht.

**CRM-Anbindung SalesSuite:** Drei Wege, genau einen wählen, sonst kommen Leads doppelt an. **(C) Direkt über die SalesSuite-API, empfohlen und gebaut:** `netlify/functions/submission-created.js` im Repo-Root. Netlify ruft die Funktion bei jeder Formular-Einsendung automatisch auf (Event `submission-created`, Dateiname fest). Sie legt per SalesSuite Public API (`api.salessuite.com/api`, Header `x-api-key`; Endpunkte aus der offiziellen n8n-Integration abgelesen) Kontakt (Firma + Ansprechpartner, `create-or-update-by-email`), Deal in der Pipeline und eine Notiz mit allen Antworten an: `foerdercheck` → Phase „Lead“, `foerdercheck-termin` → bestehender Deal auf „Termin gebucht“ (sonst neu), `foerdercheck-rueckruf` → „Rückruf gewünscht“ (ein gebuchter Termin wird nicht zurückgestuft); fremde Formulare werden ignoriert. Konfiguration nur über Netlify-Umgebungsvariablen: `SALESSUITE_API_KEY` (Pflicht, ohne Key passiert nichts), optional `SALESSUITE_PIPELINE` (Standard „Beratungsförderung“), `SALESSUITE_PHASE_LEAD` („Lead“), `SALESSUITE_PHASE_TERMIN` („Termin gebucht“), `SALESSUITE_PHASE_RUECKRUF` („Rückruf gewünscht“); Pipeline und Phasen müssen in SalesSuite mit diesen Namen existieren, die IDs holt sich die Funktion selbst. Fehler landen im Netlify-Function-Log, der Lead bleibt in jedem Fall in Netlify Forms. Firmenfeld: erst `contact.name`, bei 4xx `companyName`, zuletzt nur die Person. Getestet gegen einen Mock (`ss-mock-test.js`). Custom-Eigenschaften (Bundesland, Fördersatz …) werden noch nicht in Felder geschrieben, nur in die Notiz; dafür einmal `GET /v1/property` des Kontos ansehen und die Schlüssel eintragen. **(A) Ohne Code, Zapier:** Netlify → Forms → Notifications → *Outgoing webhook* für das Formular `foerdercheck` (Lead) und eines für `foerdercheck-termin` (Termin) auf je einen Zapier-*Catch Hook*; Netlify schickt alle Felder als JSON unter `data`, serverseitig, unabhängig von Adblockern und Tab-Schließen. **(B) Aus der Seite heraus:** in `main.js` `CONFIG.crmHookLead` / `CONFIG.crmHookTermin` eintragen; `sendCrm()` postet JSON mit `event: 'lead' | 'termin'` und allen Feldern (`vorname`, `nachname`, `name`, `firma`, `email`, `telefon`, `telefon_e164`, `bundesland`, `region`, `mitarbeiter`, `umsatz`, `branche`, `vorfoerderung`, `anliegen`, `antworten`, `quote`, `zuschuss_je_beratung`, `handel2030`, `quelle`, UTM, `gclid`, `fbclid`, `landing_page`, `referrer`, `submitted_at`, beim Termin zusätzlich `termin`, `termin_start`, `termin_ende`, `termin_titel`, `termin_uid`). Zap 1 (Lead): Catch Hook → SalesSuite *Find or Create Contact by Email* (Firma = `firma`, Person = `vorname`/`nachname`, E-Mail, Telefon, Eigenschaften Bundesland/Branche/Mitarbeiter/Umsatz/Fördersatz/Quelle) → *Create Deal* (Pipeline z. B. „Beratungsförderung“, Phase „Lead, kein Termin“, Name „Fördercheck · Firma“) → *Create Note* mit `antworten`. Zap 2 (Termin): Catch Hook → *Find Deals by Email* → *Update Deal* (Phase „Termin gebucht“, Termindatum aus `termin_start`); falls kein Deal, *Create Deal* direkt in „Termin gebucht“. Rückrufwünsche (`foerdercheck-rueckruf`) bei Bedarf als drittes Outgoing webhook.

**Ergebnisschritt (v12b):** Die unscharfen Zahlen sind jetzt die Bühne (`.result-stage` → `.result-teaser`, Platzhalter 80 % / 2.800 € / 2 / 5.600 € = öffentliche Höchstwerte, `blur(9px)`, `aria-hidden`), das Formular liegt als Pop-up darüber (`.result-modal`, `position: fixed` ab Unterkante der Kopfzeile, `--overlay-top` setzt `main.js` `placeModal()`), auf dem Handy als Sheet von unten (80 % Höhe, innen scrollbar), sodass die Felder ohne Scrollen sichtbar sind. Die Überschrift „Ihr Unternehmen kommt für die Förderung in Frage.“ steht im Pop-up (`#leadFormTitle`), die `.result-head` ist während des Pop-ups ausgeblendet (`.has-modal`) und erscheint nach dem Absenden wieder über dem Kalender.

## Dateien

| Datei | Zweck |
|---|---|
| `index.html` | Landingpage. Alle „Jetzt Förderfähigkeit prüfen“-Buttons sind Links auf `anfragen.html`. |
| `anfragen.html` | Fördercheck als eigene Seite: Fragebogen (Frage 1 optional per `?land=` vorbelegt), Rechenschritt, verdecktes Ergebnis, Lead-Formular, Rückruf-Zweitformular, Cal.com. Alle Formularfelder statisch, damit Netlify Forms sie kennt. Schließen-Kreuz führt zurück auf die Landingpage, Escape ist dort abgeschaltet. `main.js` erkennt die Seite an `data-standalone` und startet den Check sofort. |
| `assets/css/style.css` | Stylesheet. CI ausschließlich im `:root`-Block. |
| `assets/js/main.js` | Animationen, Fragebogen, Rechenlogik, Rechenschritt, Formular per `fetch`, Kalender, Tracking. Konfiguration oben im `CONFIG`-Block. |
| `assets/img/` | Bilder (WebP): Sektion „Staatlich gefördert“ mit `illu-telefon` (Erstgespräch), `illu-siegel` (Zugelassener Berater, Band in Schwarz-Rot-Gold), `illu-schild` (Geprüfte Qualität), `illu-karte` (Deutschlandweit), `illu-2016` (Seit 2016), dazu Foto-Motiv für die Bildsektion, `illu-zuschuss` (Euro-Kachel mit Haken, ersetzt seit v11 die Münzen), Dokument, Uhr, Werkstatt, `hero-bg.webp` (Poster), die sechs Themen-Illustrationen `illu-ziel`, `illu-content`, `illu-webseite`, `illu-ki`, `illu-anzeigen`, `illu-team` (Zielscheibe, Smartphone mit Play-Button, Browser mit Umschlag, KI-Würfel, Megafon, Ausweis-Badge) sowie `bundeslaender.svg` (Umriss-Sprite der 16 Bundesländer). Erzeugt mit Higgsfield (GPT Image 2.5, transparenter Hintergrund) in Weiß/Navy/Rot/Gold. |
| `assets/img/hero-loop.mp4`, `hero-loop-sm.mp4` | Motiongrafik für den Hero (Higgsfield Kling 3.0 aus dem Hero-Bild, mit ffmpeg zum nahtlosen Vor-und-zurück-Loop geschnitten, 13 s, 1280 px ≈ 2 MB und 854 px ≈ 1 MB). JS wählt die kleine Datei bis 640 px Breite; ohne JS oder bei `prefers-reduced-motion` bleibt das Poster stehen. |
| `assets/beratung-loop.mp4`, `beratung-loop-sm.mp4`, `beratung-poster.jpg` | Stummer 10-s-Loop aus dem Hero der Hauptseite (`assets/cta-hintergrund.mp4`), mit ffmpeg ohne Tonspur auf 1280 px (1,3 MB) und 960 px (0,7 MB) gerechnet; läuft in „Die Beratung“ statt des Bürofotos, wird erst geladen, wenn die Sektion in Sicht kommt (`.lazy-video` in main.js), kleine Datei bis 640 px Breite, bei `prefers-reduced-motion` bleibt das Poster. |
| `assets/logos/` | Die 20 Kundenlogos (Kopie aus `/assets/logos/` der Hauptseite) für Laufband und Logo-Raster. `hellriegel.svg` hat hier eine breitere viewBox, damit der Schriftzug nicht abgeschnitten wird. |
| `assets/kunden/`, `assets/*.jpg`, `assets/deutschland-karte.svg` | Kundenfotos (Barwig, Zotzmann, Börner, Fischer, Vogt, Rossegger, Curator, Emrich, Just Brides, Bentivegna, Hellriegel, alesi), Agenturfotos (Fabian am Podcast-Mikro, Studio, Dreh, Büro, Gebäude) und die Deutschlandkarte, Kopien von der Hauptseite. Nicht mehr verwendet, können gelöscht werden: `fabian-profil.jpg`, `jasmin-profil.jpg`, `team-kamera-check.jpg`. |
| `assets/js/agenturmarkt-responsive.js` | Kopie des Widget-Laders der Hauptseite: lädt auf dem Desktop das echte AgenturMarkt-Widget (`widget.trustmarkt.de`), zeigt auf dem Handy oder wenn das Widget nach 6 s nicht erscheint eine statische Score-Karte (4,9 · 76 Bewertungen). |
| Zweitformular `foerdercheck-rueckruf` | Statisch im Markup (versteckt), wird per `fetch` abgeschickt, wenn jemand „Lieber Rückruf“ wählt. |
| `../robots.txt` | Sperrt `/foerderung/` und `/danke` (neu angelegt, lag vorher nicht im Deploy-Ordner) |

Alle Bild- und Logopfade sind relativ (`assets/…`), der Ordner ist damit in sich geschlossen; nur `/favicon.svg` kommt vom Wurzelverzeichnis der Hauptseite.

## Sektionen (Reihenfolge)

Seit v11 gilt die Reihenfolge aus dem Block „Stand v11“ oben; die folgende Liste beschreibt die Inhalte der Sektionen. Nav-Pille · Hero (Positionierung „Unternehmensberatung für Marketing & Digitalisierung“, H1 „Bis zu 80 % Zuschuss für Ihr Marketing. Sind Sie förderfähig?“ (Coach, v11d; Eyebrow und Sub nennen weiter die Unternehmensberatung), animiertes Datums-Badge „Programm läuft bis 31.12.2026 · noch X Tage“ mit Countdown, Video-Hintergrund, rechts die Start-Karte mit Frage 1 und 16 Bundesland-Kacheln) · Logo-Laufband · **Staatlich gefördert** („Bis zu 80 % der Beratungskosten kommen nach Prüfung zurück“, vier Schritte Erstgespräch / Antrag vor Angebotsabschluss / Informationsschreiben, dann Start / Zuschuss nach Prüfung, fünf Vertrauensmerkmale inkl. Google-Partner-Badge von gstatic mit „G“-Fallback) · Für wen (Bento) · **Ergebnisse** (sechs Themen mit 3D-Illustrationen, drei große Fallstudien, Block „Wer die Förderung mit uns genutzt hat“: drei weiße Karten Sportakademie Chemnitz, Sportakademie Vogt, alesi mit Badge „Beratung mit staatlichem Zuschuss genutzt“ und nur Beratungsthemen, dazu „Und viele weitere“, dazu die Kunden-Galerie „100+ Unternehmen … Ein paar Einblicke“ von der Hauptseite) · **Die Zusammenarbeit** (was genau gefördert wird, sechs Bausteine inkl. Mitarbeitergewinnung) · **Warum die Schmidtke GmbH** (Kompetenznachweis: eigenes Studio, Zahlen, alle Bausteine, Beispiel-Monatsbericht) · **Über uns** (Agentur, Rottweil, DACH-Karte mit Pins, „Was wir für unsere Kunden umsetzen“) · Bild-CTA · Kunden-Logos (20 Logos im Raster) · **Ihr Berater** (nur Fabian, mit Werdegang) · **Bewertungen** (AgenturMarkt-Widget + Zeile 4,9 von 5 · 76 Bewertungen, sechs Sterne-Karten, drei Stimmen aus den Fallstudien) · FAQ · Abschluss · Footer · Sticky-CTA.

Das Datums-Badge rechnet die Resttage bis 31.12.2026 im Browser aus (`main.js`, Block „Countdown“) und zählt beim Laden herunter.

## Aufbau und Animationen (Vorlage: foerderung.de, vermessen per getComputedStyle)

Typo- und Abstandstokens (`:root` in style.css, abgeglichen mit foerderung.de bei 1440 px am 23.09.2026): H1 68 px, H2 56 px (Vorlage 56, Gewicht 600 dort / 700 bei uns, SK Modernist hat nur 400/700), Sektions-Sub 18 px / 1,55 / max. 660 px, Eyebrow 14 px Versalien, Sektionsabstand `clamp(64px, 6.25vw, 90px)` (Vorlage 72–81 px), Kartentitel `--card-title` 20 px, Kartentext `--card-text` 15 px, Zwischenüberschriften `--sub-title` 24–30 px, Kartenpolster `--card-pad` 28 px, Rasterabstand `--grid-gap` 20 px, Blockabstand `--block-gap` 40–64 px, Radien 20 px Karten / 14 px Kacheln / 44 px Hero. Neue Sektionen immer über diese Tokens bauen, keine Einzelwerte.

- Schwebende Nav-Pille (fixed, 62 px, radius 999), Hero als große abgerundete Karte (44 px), Sektionen mit 72–130 px Luft, Karten 20 px Radius ohne Schatten, Buttons als Pille 52–56 px mit Pfeil.
- Überschriften: `data-anim="words"` → jedes Wort wird per JS in `<span class="w">` gesetzt und blendet mit Blur 10 px → 0, 0,25 em Versatz, 600 ms, 60 ms Stagger ein.
- Blöcke: `data-anim="up"` (20 px, 600 ms), Karten: `data-anim="card"` bzw. `data-anim-group="card"` (20 px + scale .93, Feder-Kurve, 90 ms Stagger).
- Grundverzögerung 300 ms nach Eintritt in den Viewport, optional `data-delay`. Illustrationen schweben (`.float`), Bilder mit `data-parallax` bewegen sich leicht mit dem Scroll, Logo-Laufband 40 s.
- Alle Mockups (Browserfenster, Ergebniskarten, Statusliste) sind HTML/CSS, keine Bilder.
- `prefers-reduced-motion` schaltet alles ab.

## Rechenlogik (Förderrichtlinie 14.12.2022, geändert 12.12.2024)

- 3.500 € förderfähige Kosten netto je Beratung (nur intern, steht nicht auf der Seite)
- 80 %: Brandenburg, Mecklenburg-Vorpommern, Sachsen-Anhalt, Thüringen, Sachsen ohne Region Leipzig, Landkreis Lüneburg, Region Trier
- 50 %: alle übrigen inkl. Berlin und Region Leipzig
- KMU: unter 250 Mitarbeiter und höchstens 50 Mio. € Umsatz
- Ausschluss: Branchen „Coach / Trainer“, „Berater / Consultant“ und „Anwalt / Steuerberater“ (je eigene Begründung, bei Coaches/Beratern zusätzlich der Hinweis, dass wir viele solcher Anfragen bekommen und die Richtlinie das ausschließt), 250+ Mitarbeiter, über 50 Mio. € → sofortiges Ergebnis ohne Formular
- Zusatzprogramm **Zukunft Handel 2030** (Land Baden-Württemberg): Bundesland Baden-Württemberg + Branche „Einzelhandel (Ladengeschäft)“ → im Rechenschritt erscheint die Zeile „Landesprogramm wird geprüft“, nach dem Absenden des Formulars der grüne Hinweis „Zusätzlich für Sie gefunden: … weiteres Landesprogramm“ (Details erst im Gespräch: Intensivberatung im Wert von bis zu 12.000 €, 6–15 Beratungstage, 297 € netto Eigenanteil je Tag, bis 31.12.2026), im Lead das Feld `handel2030=ja`. Quellen: wm.baden-wuerttemberg.de (Förderaufruf, Zuschuss 70 %, Tagessatz bis 800 €, Laufzeit bis 31.12.2026) und Anbieterseiten (297 € Eigenanteil je Tagewerk, 6–15 Tagewerke). Die Schmidtke GmbH ist im Programm zugelassen (bestätigt 23.09.2026). Das Programm darf nicht auf der Landingpage erscheinen, nur im Ergebnis des Förderchecks.
- Branchen-Frage mit zwölf Kacheln (A–L), jede Kachel hat ein Buchstaben-Badge; der Buchstabe auf der Tastatur wählt die Kachel (gilt in allen Frageschritten, A–P). Frage 1 zeigt zusätzlich den Bundesland-Umriss aus `bundeslaender.svg`.
- Frage 6 („Woran arbeiten Sie gerade?“) hat sechs Kacheln: Mehr Anfragen gewinnen · Sichtbarkeit und Positionierung · Mitarbeiter gewinnen · Webseite und Anzeigen · Prozesse und KI im Betrieb · Etwas anderes. Die Antwort geht als Feld `anliegen` mit dem Lead.
- „Drei- oder öfter“ bei Vorförderung → Hinweis auf Kontingent (5 bis 31.12.2026, 2 pro Jahr), kein Ausschluss

## Konfiguration (`main.js`, Block `CONFIG`)

| Schlüssel | Aktuell |
|---|---|
| `calLink` | `schmidtke-gmbh/20-minuten-gesprach-am-telefon` |
| `calPhoneField` | `attendeePhoneNumber` – Slug des Telefonfelds im Cal.com-Event. Fehlt das Feld, verfällt die Nummer stillschweigend. |
| `dankeUrl` | `https://schmidtke-gmbh.de/danke?von=foerdercheck` – Weiterleitung 2,5 s nach erkannter Buchung |
| Telefonnummer | `0741 94213040` (im HTML unter dem Kalender) |

## Tracking (`dataLayer`, Feld `funnel: 'foerdercheck'`)

`funnel_step` (step 1–6) · `funnel_preset` (land, wenn der Check von einer Hero-Kachel mit `?land=` gestartet wurde) · `check_completed` (quote, foerderfaehig) · `lead_submitted` (quote) · `next_step_tab` (cal/call) · `callback_requested` · `call_booked`.
Vibetrack-Tracker und Cookie-Banner wie auf der Hauptseite eingebunden. UTM-Parameter, `fbclid`, `gclid`, Landingpage und Referrer gehen als versteckte Felder in den Lead.

## Ergebnisseite und Senden

- Nach „Ergebnis anfordern“ wird der Lead per `fetch` als `x-www-form-urlencoded` an `/` geschickt (Netlify-Konvention), bei Fehler noch einmal an die eigene Adresse. Schlägt beides fehl oder dauert es länger als 6 s, geht es trotzdem weiter zu Termin/Rückruf und ein Hinweis wird eingeblendet; Termin und Rückruf fangen den Kontakt dann auf. Auf `localhost`/`file://` wird nichts gesendet (Testmodus).
- Nach dem Absenden: kein Rechenweg und keine Zahlen (Ergebnis bleibt verdeckt), sondern „Fast geschafft“ mit zwei Wegen: **Termin direkt wählen** (Standard, Cal.com inline, im Gespräch gibt es das Ergebnis) oder **Lieber Rückruf** (schickt das Zweitformular, bestätigt „Wir melden uns in den nächsten Tagen“).
- Rechenschritt: sechs Zeilen (Standort, Fördersatz, Größe, Branche, Kontingent, Zuschuss) mit Prozentanzeige und Fortschrittsbalken, rund 4 Sekunden. Die echten Zahlen bleiben dabei verdeckt (`•• %`, `•.••• €`) und werden auch danach nicht angezeigt; sie stehen nur in den versteckten Formularfeldern.

## Netlify Forms

Formularnamen `foerdercheck` und `foerdercheck-rueckruf`. Felder: name, firma, email, telefon, datenschutz, bundesland, region, mitarbeiter, umsatz, branche, vorfoerderung, anliegen, quote, zuschuss_je_beratung, handel2030, utm_*, fbclid, gclid, landing_page, referrer, submitted_at.
Benachrichtigung in Netlify unter Forms → Notifications einrichten.

## Vor dem Livegang (Kundenseite)

1. **Cal.com-Event:** Telefonnummer als erstes Zusatzfeld anlegen, Slug in `calPhoneField` eintragen (oder Standardfeld „Telefon“ aktivieren → `attendeePhoneNumber`).
2. **Danke-Seite `/danke`:** zeigt aktuell selbst einen Cal.com-Kalender. Für Besucher aus dem Fördercheck (Parameter `?von=foerdercheck`) den Kalender ausblenden, sonst wird doppelt gebucht.
3. **Netlify Forms:** nach dem ersten Deploy unter Forms prüfen, dass `foerdercheck` und `foerdercheck-rueckruf` erkannt wurden, und die E-Mail-Benachrichtigung einrichten. Wenn „Das Senden hat nicht geklappt“ erscheint, war das Formular beim Deploy nicht erkannt (Forms-Erkennung im Netlify-Projekt aktivieren) oder die Seite lief lokal.
4. **Datenschutzerklärung:** Netlify Forms und Cal.com sind dort schon relevant, prüfen ob beide genannt sind.
5. **Fallstudienzahlen** (Barwig, Zotzmann, Börner) sind die von der Hauptseite; Freigabe der Kunden für diese Verwendung sicherstellen.
6. **Bewertungen:** Die sechs Sterne-Karten sind von agenturmarkt.de (Profil Schmidtke GmbH) übernommen, Namen, Quelle und Monat stehen dabei. Gesamtwert „4,9 von 5 · 76 Bewertungen“ ist der Stand beim Einbau und muss bei Änderung im HTML (`#kundenstimmen`, `.rating-line`) angepasst werden.
7. **Sitemap:** im Deploy-Ordner gibt es keine; falls eine angelegt wird, `/foerderung/` nicht aufnehmen.

## Test

Geprüft (Playwright, Chromium): Pfade Thüringen · Sachsen mit Leipzig · Sachsen ohne Leipzig · Baden-Württemberg · Ausschluss Berater · Ausschluss Coach · Ausschluss Anwalt · Ausschluss 250+ · Tastaturwahl · Baden-Württemberg + Einzelhandel (Handel-2030-Karte, Feld handel2030=ja); Fehlerwege (leer, falsche E-Mail, fehlender Datenschutzhaken, Haken danach sichtbar, rote Markierung weg); Zahlen vor und nach dem Freischalten identisch; keine JS-Fehler; kein waagerechtes Scrollen bei 390/900/1440 px; Kalender-Fallback bei blockiertem Cal.com-Skript.

Beim lokalen Testen einer CSS-Änderung hart neu laden (Cmd + Shift + R).
